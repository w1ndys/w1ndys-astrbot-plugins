[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# BLE001（禁止捕获宽泛异常）和 S110（try-except-pass）在本项目中有意关闭：
#
#   1. AstrBot 插件开发规范明确要求「良好的错误处理机制，不要让插件因一个错误而崩溃」；
#   2. 插件跑在群聊里，一个群消息处理失败不应该把整个插件带下去。
#
# 因此这里的宽泛捕获是刻意的设计，而不是疏漏。
ignore = ["BLE001", "S110"]
